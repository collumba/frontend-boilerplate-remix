/**
 * Toast Feature Constants
 *
 * Shared constants, enums, and type definitions
 */

// Toast types enum
export enum ToastType {
  SUCCESS = 'success',
  ERROR = 'error',
  WARNING = 'warning',
  INFO = 'info',
  DEFAULT = 'default',
}
